@AGENTS.md

<!--
Claude Code reads CLAUDE.md and not AGENTS.md, so this imports it rather than
duplicating it. A symlink works too, but needs Administrator or Developer Mode
on Windows, which this repo treats as a supported platform.

HTML comments are stripped before the file reaches Claude's context, so this
note is free. Claude-specific instructions would go below the import.
-->

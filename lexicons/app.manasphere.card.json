{
  "lexicon": 1,
  "id": "app.manasphere.card",
  "defs": {
    "main": {
      "type": "record",
      "description": "A card you own, in however many identical copies.",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["scryfallId", "finish", "quantity", "createdAt"],
        "properties": {
          "scryfallId": {
            "type": "string",
            "maxLength": 64,
            "description": "Scryfall print id: the exact printing, which also pins the language."
          },
          "finish": {
            "type": "string",
            "maxLength": 32,
            "knownValues": ["nonfoil", "foil", "etched"],
            "description": "Part of what identifies these copies; foil and nonfoil are separate records."
          },
          "quantity": {
            "type": "integer",
            "minimum": 1,
            "maximum": 10000,
            "description": "Copies owned now. Deleted rather than kept at zero."
          },
          "condition": {
            "type": "string",
            "maxLength": 32,
            "knownValues": [
              "mint",
              "nearMint",
              "excellent",
              "good",
              "lightPlayed",
              "played",
              "poor"
            ],
            "description": "The Cardmarket scale, as ManaBox exports. Absent means ungraded."
          },
          "container": {
            "type": "string",
            "format": "at-uri",
            "description": "The container holding these copies. Absent means unfiled."
          },
          "acquisitions": {
            "type": "array",
            "maxLength": 64,
            "items": { "type": "ref", "ref": "#acquisition" },
            "description": "How these copies were come by. History, so it need not sum to quantity."
          },
          "proxy": {
            "type": "boolean",
            "description": "Not a real card: fills a deck slot but counts toward no valuation."
          },
          "note": {
            "type": "string",
            "maxLength": 3000,
            "maxGraphemes": 300,
            "description": "Free text, and world-readable like every other field."
          },
          "tags": {
            "type": "array",
            "maxLength": 32,
            "items": {
              "type": "string",
              "maxLength": 64,
              "knownValues": ["altered", "misprint", "signed"]
            },
            "description": "Free-form labels. Moxfield exports these."
          },
          "createdAt": { "type": "string", "format": "datetime" },
          "updatedAt": {
            "type": "string",
            "format": "datetime",
            "description": "Client-declared, so it can be stale: a PDS records no modification time."
          }
        }
      }
    },
    "acquisition": {
      "type": "object",
      "description": "One lot of copies, and what it cost.",
      "required": ["quantity"],
      "properties": {
        "quantity": {
          "type": "integer",
          "minimum": 1,
          "maximum": 10000
        },
        "at": {
          "type": "string",
          "format": "datetime",
          "description": "Absent when the source didn't record it."
        },
        "price": {
          "type": "string",
          "maxLength": 32,
          "description": "What you paid per copy, as a decimal string. Absent means you didn't say."
        },
        "currency": {
          "type": "string",
          "minLength": 3,
          "maxLength": 3,
          "description": "ISO 4217 for price."
        },
        "marketValue": {
          "type": "string",
          "maxLength": 32,
          "description": "What a copy was worth when the row was written."
        },
        "marketCurrency": {
          "type": "string",
          "minLength": 3,
          "maxLength": 3,
          "description": "ISO 4217 for marketValue. Scryfall quotes USD and EUR."
        }
      }
    }
  }
}
